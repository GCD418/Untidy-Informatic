#include <bits/stdc++.h>
using namespace std;
long long t, s, food;

long long func()
{
    while(s>=1)
    {

    }
}

int main()
{
    cin>>t;
    while(t--)
    {
        food=0;
        cin>>s;
    }
    return 0;
}
