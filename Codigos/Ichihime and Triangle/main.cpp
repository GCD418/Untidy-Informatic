#include <bits/stdc++.h>
using namespace std;
long long t, a, b, c, d, x, y, z, i;

long long func()
{
    x=b;
    y=b;
    z=x+y;
    if(z<=d)
    {
        cout<<x<<" "<<y<<" "<<z<<endl;
        return 0;
    }
    if(z)

    return 0;
}

int main()
{
    cin>>t;
    while(t--)
    {
        cin>>a>>b>>c>>d;
        func();
    }
    return 0;
}
