#include <bits/stdc++.h>
using namespace std;
long t, n;
int main()
{
    cin>>t;
    while(t--)
    {
        cin>>n;
        long br[n], neck[n];
        for(int i=0; i<n; i++)
        {
            cin>>br[i];
        }
        for(int i=0; i<n; i++)
        {
            cin>>neck[i];
        }
        for(int i=0; i<n; i++)
        {
            for(int j=0; j<n; j++)
            {

            }
        }
        /*sort(br, br+n);
        sort(neck, neck+n);
        reverse(neck, neck+n);
        for(int i=0; i<n; i++)
        {
            cout<<br[i]<<" ";
        }
        cout<<endl;
        for(int i=0; i<n; i++)
        {
            cout<<neck[i]<<" ";
        }
        cout<<endl;*/
    }
    return 0;
}
