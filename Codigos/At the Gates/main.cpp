#include <bits/stdc++.h>
using namespace std;
long long t, n, k, restar, heads;

int main()
{
    cin>>t;
    while(t--)
    {
        heads=0;
        cin>>n>>k;
        char s[n];
        restar=(n-k-1);
        for(int i=0; i<n; i++)
        {
            cin>>s[i];

        }

        cout<<heads<<endl;
    }
    return 0;
}
